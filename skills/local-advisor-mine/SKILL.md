---
name: advisor-mine
description: Намайнить НОВЫХ кандидатов в advisors (PaloAlto AI Research Lab Knowledge) и дописать их в воронку, чтобы она не пересыхала — верхнее звено advisor-конвейера. Триггеры "/advisor-mine", "намай эдвайзеров", "намай advisors", "кто ещё в advisors", "пополни воронку эдвайзеров", "advisor mining", "mine advisors". READ-ONLY по людям — НИКОМУ не пишет (аутрич делает отдельная рутина advisor-outreach-daily). Канон: память peer-export-lab-knowledge, proactive-lead-copilot, outreach-journal-and-daily-mandate.
---

## ⚖️ ШАГ 0 — СНАЧАЛА БИБЛИЯ (обязательно, origin: anton 2026-07-26)

Перед ЛЮБЫМ действием этого скилла подними выстраданные правила Библии по лидам:

```bash
python3 ~/.claude/scripts/bible_leads.py            # карта (0 токенов)
python3 ~/.claude/scripts/bible_leads.py --grep <тема>   # срез
```

Идёт РАНЬШЕ `outreach_log.py check` и раньше RECALL по человеку. Нет правила в Библии →
это находка, после работы занести через `/intake`, а не импровизировать молча.
Канон: `reglament-lyubaya-rabota-s-lidami-snachala-bibliya` · `_Bible-Outreach-MOC` · CLAUDE.md §9.5

Ты — майнер кандидатов в advisors Антона. Твоя работа — **находить НОВЫХ людей** и класть их в ту же воронку, что читает аутрич-рутина, чтобы конвейер не встал. Ты ничего и никому НЕ отправляешь.

## Место в конвейере (перелинковка — это одна общая воронка)

```
  ┌─ MINE (ты, /advisor-mine · weekly) ─────────────────────────────┐
  │  07-People греп + TG common_groups + FB-комментаторы + gold-лиды │
  │  → дедуп (журнал + уже-в-воронке) → НОВЫЕ карточки               │
  └───────────────┬─────────────────────────────────────────────────┘
                  ▼  дописываю в ОДИН файл:
        _Dashboards/Advisor-Funnel.html   ← общий контракт
                  ▼  тот же файл читает:
  ┌─ OUTREACH (advisor-outreach-daily · 5 инвайтов/день) ───────────┐
  │  берёт следующих из воронки → персональный инвайт → журнал        │
  └───────────────┬─────────────────────────────────────────────────┘
                  ▼  «да» → приватный GitHub-репо
  ┌─ INTAKE (advisor-intake · launchd 05:30) ──────────────────────┐
  │  ловит PR/issues советов → вердикт мерж/отклон/эпизод            │
  └─────────────────────────────────────────────────────────────────┘
```
**Перелинковка = я дописываю НОВЫХ в тот самый `Advisor-Funnel.html`, из которого outreach-daily берёт следующих.** Ничего параллельного не создаю.

## Движок (0 токенов, детерминизм — грунт на Sonnet)
`~/Obsidian/Anton-Knowledge/_outreach/advisor_mine.py`
- `scan [--min-score N] [--limit N] [--json]` — прочёсывает 07-People, ловит сигнал (роль 1 строит агентов/Claude Code · роль 2 B2B-продажи/GTM · роль 3 DevRel/контент), отсеивает активных лидов Platinum (`status` negotiating/qualifying/proposed/won) и штрафует чистых sales-`lead`, дедупит по воронке+журналу, ранжирует НОВЫХ.
- `append <slug1> <slug2> …` — дописывает отобранных в `Advisor-Funnel.html` как `candidate`-строки.

## Маршрут (детерминизм → суждение → запись)

**1. RECALL + грунт-скан (Sonnet/0-токенов).**
- `python3 ~/Obsidian/Anton-Knowledge/_outreach/advisor_mine.py scan --min-score 6 --json` — база кандидатов из 07-People.
- (опц.) добери живые рельсы, если Антон просит глубже:
  - **TG-сообщества**: MCP `search_contacts` / `get_common_chats` по AI-группам (AI Agents Founders, OpenClaw/SanFranciscoAI automation, On vAIbe) — кто активно строит на Claude/LLM.
  - **FB-комментаторы**: кто комментит посты Антона про Claude/автоматизации (pubmetrics / fb-корпус) — тёплая техно-аудитория.
  - **gold-лиды с тегом builder**, ещё НЕ в воронке.

**2. Суждение (Opus).** Из скана оставь только тех, кто РЕАЛЬНО живёт в Claude/LLM и годится в advisors:
- ✅ строит с Claude/LLM/агентами · DevRel/техно-аудитория · B2B-продажи/GTM экспертиза;
- ✅ **реципрокность**: `relationship` = partner/peer-founder/role_model (равный обмен), не тот, кому Антон САМ питчил продукт (это клиент, не эдвайзер) и не активный sales-`lead`;
- ✅ ок с публичностью (фаундер/деврел/публичный автор);
- ❌ отсей: боты/амбассадоры чужих воронок, чистые крипто-спекулянты без строительства, активные лиды Platinum.
- Проверь доказательства в `07-People/<slug>.md` (grep claude/agent/devrel/relationship) — 2 доказательства на кандидата.

**3. Дедуп (обязательно перед записью).** Для каждого финалиста:
`python3 ~/Obsidian/Anton-Knowledge/_outreach/outreach_log.py check "<Имя>"` → должно быть `CLEAN`. Скан уже дедупит по воронке+журналу, это финальная страховка.

**4. Запись.** Бэкап воронки (`cp Advisor-Funnel.html Advisor-Funnel.html.bak-…`), затем
`python3 advisor_mine.py append <slug…>` — дописывает НОВЫХ в воронку.
Роль + 30-дневный аск описывай в карточке (формат разового `Advisor-Candidates.html`): роль (1/2/3) · 2 доказательства · что просим на 30 дней (смотрит систему → говорит где обманываемся; взамен забирает скиллы/архитектуру; денег никто никому).

**5. Отчёт в TG-03** (`-[id]`, аккаунт [рабочий аккаунт]):
`🤖 [[машина флота]] ⛏️ advisor-mine: +N новых в воронку (имена) · всего кандидатов K`.

## Границы
- **READ-ONLY по людям — НИКОМУ не пишу.** Инвайты шлёт `advisor-outreach-daily` по мандату «5/день».
- Только НЕ активные лиды Platinum (движок фильтрует по status + штрафует sales-lead; на суждении добиваю по relationship).
- Модель: грунт-скан = Sonnet (бесплатный бак), отбор/доказательства = Opus.
- Tier-2 (новый публичный канал, деньги, обещания) → к Антону в 02.
- Еженедельно (рутина `advisor-mine-weekly`, воскресенье ночью) — новые люди не появляются каждый день (AK-47).

## /tt самопроверка
Идемпотентность: после `append` повторный `scan` НЕ должен выдавать тех же людей (дедуп по воронке). Плохой slug / пустой ввод — graceful. Только тогда «готово».
