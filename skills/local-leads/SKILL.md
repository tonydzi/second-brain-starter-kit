---
name: leads
description: >-
  Проактивный лид-радар [машина флота] — живьём поднять лидов, которым нужен ХОД, и принести Антону
  готовые след.шаги, чтобы он приходил на готовое, а не искал. Собирает сигналы детерминированно
  (журнал аутрича + GitHub advisor-воронка + CRM-Lite остывающие) → дашборд + по «+» готовит
  черновики ответов (draft-first, тон по каналу, голос Антона=Opus). Триггеры: "/leads",
  "лид-радар", "кто ждёт хода", "подними лидов", "кто остывает", "работаем лидов", "lead radar",
  "who needs a move". Сбор READ-ONLY; отправка лидам — ТОЛЬКО по явному «+» Антона (кроме
  advisor-funnel 5/день, у него своя рутина). НЕ путать с /pipeline (хабовый tg_followups +
  исполнение отправки) — /leads = [машина флота]-локальный радар + подготовка черновиков. Канон:
  память proactive-lead-copilot, outreach-journal-and-daily-mandate, anton-sales-avoidance-root.
---

## ⚖️ ШАГ 0 — СНАЧАЛА БИБЛИЯ (обязательно, origin: anton 2026-07-26)

Перед ЛЮБЫМ действием этого скилла подними выстраданные правила Библии по лидам:

```bash
python3 ~/.claude/scripts/bible_leads.py            # карта (0 токенов)
python3 ~/.claude/scripts/bible_leads.py --grep <тема>   # срез
```

Идёт РАНЬШЕ `outreach_log.py check` и раньше RECALL по человеку. Нет правила в Библии →
это находка, после работы занести через `/intake`, а не импровизировать молча.
Канон: `reglament-lyubaya-rabota-s-lidami-snachala-bibliya` · `_Bible-Outreach-MOC` · CLAUDE.md §9.5

## 📐 Очерёдность триажа (Библия, принято anton 27.07)

Порядок разбора лидов — не «по чуйке», а по канону `protocol-investor-lead-pipeline`:
1. Входящие от **verified** лидов (ответил/жив) — вкл. проверку «звонок >3–6 мес назад → touch base по ритму, повод не нужен»;
2. Подозрение на инвестора/сильного — уточнить статус;
3. Verified без непрочитанных;
4. B2B / KOL;
5. Нераспознанные.
Свод целиком: `python3 ~/.claude/scripts/bible_leads.py` (ядро пп. 9–12: touch-base, триаж, no-show, DoD звонка).

# /leads — проактивный лид-радар ([машина флота])

> 🧒 **Отчёт Антону:** заканчивай простым «Простыми словами: …». НИКОГДА внутри сообщений лидам.
> 📖 Под скиллом `bible` (аутрич-кодекс `_Bible-Outreach-MOC`). Отправка = только по «+» Антона;
> деньги/обязательства/секреты/масс-флуд → пауза+спрос.

Мандат Антона 2026-07-11: «мне нужно чтобы ты сам максимально проактивно помогал работать с лидами».
Антон продаёт прекрасно, когда включён, но избегает экспозиции ([[anton-sales-avoidance-root]]) —
моя работа снимать трение и приносить готовое.

## Шаг 1 — Собрать радар (детерминированно, 0 LLM)
```
python3 ~/Obsidian/Anton-Knowledge/_outreach/lead_radar.py
```
Строит `_Dashboards/Lead-Radar.html` и печатает сводку. Группы:
- 🔴 **Ждут Антона лично** — ответил-без-followup · остывающий gold-лид · нужен звонок/решение. Рука Антона.
- 🟡 **Черновик готов, жми** — тишина после отправки · инвайт не принят · в очереди. Готовлю драфт.
- ⏳ **Наблюдаем** — свежее (<3д), ждём ответ. Ничего не делаем, следим.

Открой дашборд Антону (визуал первым — он работает глазами). Флаги источников:
`--no-github` если gh оффлайн; `--digest` — компактный дайджест; `--json` — машинный.
⚠️ Если печатает «GitHub-источник недоступен» — advisor-воронка неполная, скажи об этом.

## Шаг 2 — Обогатить RECALL по каждому 🔴/🟡
Перед предложением хода подними контекст (не пиши вслепую):
- CRM-Lite карточка: `~/Obsidian/PaloAlto-AI-Research-Lab-Knowledge/crm-lite/leads/<slug>.md` (org/role/next step).
- История TG: тред лида (Telegram MCP, аккаунт из журнала; см. `telegram-howto`) — читать на неотвеченное.
- Журнал: `python3 ~/Obsidian/Anton-Knowledge/_outreach/outreach_log.py check <имя>` — что уже слали.

## Шаг 3 — Подготовить черновики (draft-first) на «+»
Для 🟡 (и 🔴, где уместен текст) — по одному черновику след.шага:
- **Тон по каналу**: TG/мессенджер = осмысленно, с контекстом и историей человека; коротко, без воды.
- **Голос Антона = только Opus** (авторский голос). Открытие = искренний интерес с конкретной отсылкой
  к нашей истории с человеком; CTA обязателен; первая строка — только приветствие (Библия, outreach-message-format).
- Показать Антону таблицу: **лид · сигнал · предложенный ход · точный текст черновика**.

## Шаг 4 — Отправка ТОЛЬКО по явному «+» Антона
- Antон сказал «+» на конкретное сообщение → отправить (аккаунт = самый тёплый тред) → сразу
  `outreach_log.py register --status sent` (Connect: «отправил» без записи = не отправлял).
- Advisor-funnel (5/день) — своя рутина `advisor-outreach-daily`, не дублировать здесь.
- 🔴 «звони/тёплое» — не текст от меня: подать Антону «звони, вот повод», рука его.
- Деньги/обязательства/секреты → пауза+спрос (Tier-2).

## Ритм
Утренняя рутина `lead-radar-daily` (launchd ~08:05 Лиссабон) сама пушит дайджест «кто ждёт тебя
сегодня» в чат 02. `/leads` = тот же радар живьём + подготовка черновиков в любой лид-сессии.

## Границы
- Сбор read-only; ничего не шлёт лидам без «+».
- tg_followups.json живёт на хабе — это НЕ источник /leads (он [машина флота]-локальный: ledger+GitHub+CRM-Lite).
  Для хабовой воронки Platinum → скилл `/pipeline`.
- Заканчивай 🧒-recap Антону.
